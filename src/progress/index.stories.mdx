import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import '../shared/critical-path.scss';
import Progress from './';

<Meta title="Core|Progress" component={Progress} />

# Progress bar component

N.b., this is intended for the Feature layout style.

It does not currently mirror `o-stepped-progress`.

If `value` is provided (decimal number between 0 and 1), that percentage
will be used.

If `steps` is provided, it will be a stepped progresss bar.

<Preview>
  <Story name="Examples">
    <>
      Percentage:
      <div className="dark" style={{ color: 'white' }}>
        <h3>0%</h3>
        <Progress value={0} />
        <h3>30%</h3>
        <Progress value={0.3} />
        <h3>60%</h3>
        <Progress value={0.6} />
        <h3>100%</h3>
        <Progress value={1} />
      </div>
      Stepped:
      <div className="dark" style={{ color: 'white' }}>
        <h3>0%</h3>
        <Progress value={0} steps={4} />
        <h3>30%</h3>
        <Progress value={0.3} steps={10} />
        <h3>60%</h3>
        <Progress value={0.6} steps={2} />
        <h3>100%</h3>
        <Progress value={1} steps={50} />
      </div>
    </>
  </Story>
</Preview>

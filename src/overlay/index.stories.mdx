import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean } from '@storybook/addon-knobs';
import Overlay from './';

export const defaultProps = (isShowing) => ({
  isShowing: boolean('Toggle overlay', isShowing),
});

<Meta title="Core|Overlay" component={Overlay} decorators={[withKnobs]} />

# Overlay component

<Preview>
  <Story name="Example">
    <div>
      Use the Knobs toggle to show overlay
      <Overlay {...defaultProps(false)}>Overlay content</Overlay>
    </div>
  </Story>
</Preview>

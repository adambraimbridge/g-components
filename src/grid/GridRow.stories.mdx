import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { GridContainer, GridRow, GridChild } from './';

<Meta title="Core|Grid/GridRow" component={GridRow} parameters={{ chromatic: { disable: true } }} />

# GridRow component

`<GridRow />` is used to create responsive groups of `<GridChild />` columns.

It has one config option: `compact`, which will remove gutters from columns

<Preview>
  <Story
    name="Compact and gutterless columns"
    parameters={{ styles: [Promise.resolve(require('./_stories.scss'))] }}
  >
    <GridContainer>
      <GridRow compact={true}>
        <GridChild colspan="6">
          <div className="demo-cell">no gutters (applied using o-grid-row--compact)</div>
        </GridChild>
        <GridChild colspan="6">
          <div className="demo-cell">no gutters (applied using o-grid-row--compact)</div>
        </GridChild>
      </GridRow>
      <GridRow compact={true}>
        <GridChild colspan="6 push6">
          <div className="demo-cell">First in source</div>
        </GridChild>
        <GridChild colspan="6 pull6">
          <div className="demo-cell">Last in source</div>
        </GridChild>
      </GridRow>
    </GridContainer>
  </Story>
</Preview>

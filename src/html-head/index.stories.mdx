import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

<Meta title="Core|HTML Head" />

# HTML header component

`HtmlHead` was originally used to get metadata into the `<head>` section of the page.

It is no longer in use. **@TODO remove.**

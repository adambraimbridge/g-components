import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import '../../../shared/critical-path.scss';
import ConstituencyLookup from './';
import { csvParse } from 'd3-dsv';
import candidates from 'raw-loader!../sample-data/candidates.csv';
import constituencies from 'raw-loader!../sample-data/constituencies.csv';

export const candidateList = csvParse(candidates);

export const constituencyList = csvParse(constituencies);

<Meta title="Elections|GE2019/Constituency Lookup" component={ConstituencyLookup} />

# Constituency Lookup component

`<ConstituencyLookup />` can be used to...

<Preview>
  <Story name="Basic">
    <ConstituencyLookup
      constituencyList={constituencyList}
      candidateList={candidateList}
      setOpenConstituency={c => window.alert(`Constituency ID: ${c}`)}
    />
  </Story>
</Preview>

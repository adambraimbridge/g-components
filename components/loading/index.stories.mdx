import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import Loading from './';

<Meta title="Core|Loading" component={Loading} />

# Loading component

Spinner for loading. Uses o-loading@^2.

<Preview>
  <Story name="Basic with no props">
    <Loading />
  </Story>
</Preview>

<Preview>
  <Story name="Basic with light theme">
    <div style={{ background: '#262a33' }}>
      <Loading theme="light" />
    </div>
  </Story>
</Preview>

<Preview>
  <Story name="All sizes with dark theme">
    <div>
      <Loading size="small" />
      <Loading size="medium" />
      <Loading size="large" />
    </div>
  </Story>
</Preview>

<Preview>
  <Story name="All sizes with light theme">
    <div style={{ background: '#262a33' }}>
      <Loading theme="light" size="small" />
      <Loading theme="light" size="medium" />
      <Loading theme="light" size="large" />
    </div>
  </Story>
</Preview>
